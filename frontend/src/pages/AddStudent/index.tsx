import { Link } from "react-router-dom";

export function AddStudent() {
    return(
        <div className="App">
            <Link to="/">go Home</Link>
            "this is the add page :)"
        </div>
    );
}